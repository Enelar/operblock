<%= this.chain(function()
{
  var that = this;

  var submit_button = 
    that.first()
      .nextAll("button")
      .detach();

    that.first().parent()
      .find("[data-mark='modal-foot'] button[role='close']")
      .before(submit_button);
})
%>