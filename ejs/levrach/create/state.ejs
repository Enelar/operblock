<%= this.chain(function()
{
  debugger;
}) %>

<%
var modal_obj =
{
  id : 'state_modal',
  title : 'Предоперационный эпикриз',
  content : function()
  {
    return this.DeferRender('levrach/create/state_modal', {})
  },
};
%>
<%= this.DeferRender
(
  function(data)
  {
    if ($('#state_modal').size())
      return 'utils/empty';
    return 'utils/modal';
  },
  modal_obj)
%>