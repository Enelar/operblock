<div class='operblock_trigger' />

<%
window.ignite = function()
{
  var target = $(".doctorroom-contentPanel").first();
  operblock.LevrachRender(target, operblock.TryToExtractIB());  
};

this.Defer(function()
{
  this.first().click(ignite);
});

%>

<%= this.DeferRender('styles', {}) %>